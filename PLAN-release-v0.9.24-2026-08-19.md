# План выпуска Trophy Navigator Desktop v0.9.24

**Дата и время:** 19.08.2026, 00:32:58 MSK  
**Автор:** Codex (локальный агент Andre)  
**Статус:** согласован прямым поручением Andre выпустить обновление для всех и обновить проектные копии.

## Проверенный объём

1. Влить PR #2 Максима без посторонних файлов: развёрнутое окно, адаптивный тулбар, крупные зоны выбора точек и изменение формы незаблокированного трека мышью.
2. Поднять версию приложения до `0.9.24` и обновить release notes.
3. Исправить релизный workflow: вместе с AppImage и Windows EXE атомарно публиковать публичный `.deb`, сверять SHA-256 и HTTP 200 до публикации updater manifest.
4. Запустить доступные тесты и CI, затем проверить GitHub Release, updater manifest и публичные артефакты.
5. Обновить витрину сайта, документацию Alpha-KM, журнал команды и YouGile либо резервную очередь синхронизации.

## Ограничения и проверки

- Не переносить старые DE2-адреса и не менять серверный контур.
- Manifest публикуется последним, после проверки всех updater-артефактов.
- Локальный `cargo check` невозможен без Rust toolchain; обязательная сборка всех платформ выполняется GitHub Actions.
- Грязную рабочую копию сайта не перезаписывать: изменения сайта выполнять через чистую временную рабочую копию и отдельно сообщить о локальных пользовательских удалениях.
