# План: сведение UI Trophy Navigator Desktop с правками Максима

**Дата и время:** 17.08.2026, 15:15:38 MSK  
**Автор:** Тим (локальный Codex-агент Andre)  
**Статус:** исследование завершено; реализация заблокирована до получения исходного diff Максима и отдельного подтверждения этого плана Andre.

## Что установлено

1. Канонический Desktop находится в `~/Projects/trophy-navigator`, `main` = `d040d0f`, опубликован тег `v0.9.23`.
2. В `~/Projects/Alpha-KM/Trophy Navigator/` исходников приложения нет. Там лежат только проектные документы и Git-гид для Максима.
3. В GitHub нет ветки `maxim/*`, Pull Request или нового commit Максима. Единственные удалённые ветки — `main`, старая `fix/responsive-ui` и уже слитая CI-ветка.
4. Документ `Гид — Git и релизы Trophy Navigator Desktop (для Максима).md` прямо фиксирует, что рабочая копия Максима разошлась с `main`, а его изменения пока жили только на его ноутбуке.
5. Описанные в гиде элементы — чекбоксы точек, «Удалить выбранные», экспорт `.wpt` и `.gpx` — уже существуют в каноническом `main` с марта 2026 года. Переносить их повторно без diff опасно: можно вернуть старые сетевые адреса, старую версию UI или сломать экспорт, усиленный в `0.9.21`.

## План объединения

### Этап 0. Получить воспроизводимый исходник Максима

Предпочтительный вариант: Максим делает ветку `maxim/<тема>` от своей текущей копии и отправляет её в `andmiro256-cyber/trophy-navigator` либо передаёт Git bundle/patch с базовым commit.

Минимально необходимое:

- SHA базового commit, от которого начиналась работа;
- SHA итогового commit или patch;
- изменённые файлы без `target/`, логов, локальных данных и секретов;
- короткий список ожидаемых визуальных и поведенческих отличий.

### Этап 1. Трёхстороннее сравнение

1. Определить базу рабочей копии Максима через `git merge-base`.
2. Отделить его новые UI-решения от уже существующих функций и старого кода.
3. Отбросить из переноса устаревшие версии, DE2 URL, старый release workflow и любые изменения вне согласованного UI-scope.
4. Сформировать точный diff и передать Andre на подтверждение до изменения канонического приложения.

### Этап 2. Реализация после GO Andre

1. Создать ветку от актуального `main`.
2. Перенести только уникальные правки Максима небольшими логическими commit.
3. Сохранить сетевой контур `https://trophynav.ru`, исправленную классификацию слоёв и надёжный экспорт `0.9.21+`.
4. Версию и release workflow на этом этапе не менять.

### Этап 3. Проверка

Обязательный минимум:

- синтаксис всех inline JavaScript-блоков;
- `node tests/desktop-export-reliability.test.mjs`;
- `cargo +stable fmt --check` и `cargo +stable check`;
- локальный запуск Tauri/AppImage;
- smoke списка точек: одиночный и массовый выбор, удаление, экспорт `.wpt`/`.gpx`, отмена сохранения;
- проверка отсутствия запросов на `87.120.84.254`;
- визуальное сравнение с эталоном Максима на широком и компактном окне.

### Этап 4. Ревью и релиз

1. QA/sign-off Тома по artifact bundle.
2. Визуальная приёмка Andre.
3. Отдельный релиз не ниже `v0.9.24` только по явной команде Andre.
4. После успешного релиза — публичный manifest/artifact smoke, обновление витрины и проектной памяти.

## Текущий блокер

На этом ноутбуке и в общих Git-контрах отсутствует рабочий diff Максима. До его получения точное «приведение к одному виду» технически не определено; реконструировать UI по устному описанию нельзя без риска потерять уже выпущенные исправления.
