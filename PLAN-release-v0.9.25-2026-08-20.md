# План выпуска Trophy Navigator Desktop v0.9.25

**Дата и время:** 20.08.2026, 19:14:05 MSK  
**Автор:** Codex (`/root`)  
**Статус:** согласован пользователем командой «делай» 20.08.2026.

## Проверенный объём

1. Объединить актуальный `origin/main` с пятью проверенными исправлениями ветки `maxim/ui-toolbar-checkboxes-track`, не откатывая уже выпущенную `v0.9.24`.
2. Выпустить улучшения очистки карты, редактирования треков, цветов объектов, светлой/тёмной темы, доступности интерфейса и отображения старта/финиша маршрута по дорогам.
3. Поднять версию приложения до `0.9.25` во всех пользовательских и сборочных метаданных и подготовить release notes.
4. Повторно выполнить JavaScript- и Rust-проверки, затем отправить релизный коммит в `main` и тег `v0.9.25`.
5. Дождаться GitHub Actions, проверить GitHub Release, updater manifest, подписи, публичные артефакты и страницы скачивания на `trophynav.ru`.

## Ограничения и проверки

- Релиз `v0.9.24` подтверждён как опубликованный и остаётся базой нового выпуска.
- В релиз входят только пять коммитов после ранее влитого PR #2 и релизные метаданные `v0.9.25`.
- Исправления прошли локальные тесты и повторное ревью Сони с результатом GO без P0/P1.
- Updater manifest считается опубликованным только после появления всех подписанных артефактов и HTTP 200 на публичных ссылках.
- Публичные страницы Alpha-KM должны показывать `v0.9.25`; перед ручными правками обязательны резервные копии.
